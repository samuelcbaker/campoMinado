package com.example.campominado.model

import android.widget.LinearLayout
import com.example.campominado.utils.Constants

class Field( screenSize: Int, density: Float, columns: Int ){
    init {
    }
}